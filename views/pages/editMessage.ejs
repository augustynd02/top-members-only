<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>New message</title>
    <link rel="stylesheet" href="/header.css">
    <link rel="stylesheet" href="/main.css">
    <link rel="stylesheet" href="/editMessage.css">
    <link rel="stylesheet" href="/footer.css">
</head>
<body>
    <%- include('../partials/header') %>
    <main>
        <section>
            <h2>Edit message "<%= message.title %>"</h2>
            <form action="/messages/<%= message.id %>/new" method="POST">
                <label for="title">Title</label>
                <input type="text" name="title" placeholder="<%= message.title %>">

                <label for="message">Message</label>
                <textarea name="message" rows="10" placeholder="<%=message.message%>"></textarea>

                <input type="hidden" name="id" value='<%= message.id %>'>

                <div class="buttons">
                    <button type="submit">Edit</button>
                    <button type="button" onclick="window.location.href='/messages'">Go back</button>
                </div>
            </form>
        </section>
    </main>
    <%- include('../partials/footer') %>
</body>
</html>
